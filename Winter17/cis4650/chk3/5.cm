int gcd (int u?????, int v ) {
    if(v == 0)?
        retu??rn u;
    else
        return /gcd(v, u -u/v*v);
}
/
void main (void) {
    int x;
??    int y;
    x = i/nput(); y = input();
    output""( gcd(x??, y) );
}
