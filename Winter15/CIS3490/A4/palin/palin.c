#include <stdio.h>
#include <stblib.h>
#include <string.h>

int main() {
	char myString[] = "";
	int len = strlen(myString);
	char palindrome[len] = "";
	int palinSize = 0;
	int i, j;


	for( i = 0; i < len; i++ ) {
		for( j = 0; j < len / 2; j++ ) {
			
		}
	}


	return 0;
}